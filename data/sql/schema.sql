CREATE TABLE products (id BIGINT AUTO_INCREMENT, type VARCHAR(255), company VARCHAR(255) NOT NULL, category VARCHAR(255) NOT NULL, logo VARCHAR(255), url VARCHAR(255), description TEXT, among VARCHAR(6), token VARCHAR(255) UNIQUE, PRIMARY KEY(id)) ENGINE = INNODB;
